pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.go"
      run: gofmt -w {staged_files} && git add {staged_files}
    lint:
      glob: "*.go"
      run: sh -c 'echo {staged_files} | xargs -n1 dirname | sort -u | xargs -I{} golangci-lint run {}'
